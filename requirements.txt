# ARGUS — Python Dependencies
# conda activate argus && pip install -r requirements.txt

# Graph database
neo4j==5.19.0

# Local LLM (Ollama runs as a separate process — this is just the Python client)
ollama==0.2.1

# Agents & orchestration
langchain==0.2.6
langchain-community==0.2.6
langgraph==0.1.19

# Vector store & embeddings
chromadb==0.5.3

# Data & utilities
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.7.4
tenacity==8.3.0
rich==13.7.1

# MITRE ATT&CK (STIX format)
stix2==3.0.1
mitreattack-python==3.0.4

# Evaluation & metrics
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0

# Dev
pytest==8.2.2
ipykernel==6.29.4

# GraphRange additions
docker==7.1.0          # Docker SDK for Python (supervisor container management)
flask==3.0.3           # Supervisor HTTP API (lightweight, no langchain)
beautifulsoup4==4.12.3 # Tool crawler HTML parsing (Kali tools listing)

# Narrowing engine — clause splitting (CPU-only, not a model call)
spacy==3.7.5
# after installing: python -m spacy download en_core_web_sm

# GraphRange Scanner additions
psutil==7.2.2   # telemetry.py CPU/RAM sampling (already present, pinning the
                # real installed version rather than the spec's 5.9.8 guess)
tiktoken==0.7.0 # telemetry.py real token counting
reportlab==4.1.0 # scanner_report.py PDF generation
pyyaml==6.0.3    # victim_builder.py compose file parsing (already present,
                 # pinning the real installed version)
